# @package _global_
experiment: seed_sweep
descriptor: nll_soft_pretrained

defaults:
  - override /born: legendre/d15D40c64
  - override /dataset: ucr_ts/italypowerdemand
  - override /trainer/classification: null
  - override /trainer/ganstyle: null
  - override /trainer/adversarial: null
  - override /tracking: online

model_path: null

tracking:
  seed: 42

trainer:
  generative:
    max_epoch: 1000
    batch_size: 128
    patience: 300
    stop_crit: "acc"
    watch_freq: 0
    save: true
    norm_control:
      target: "n_features"
      hard_every: 0
      soft_strength: ???  # FILL FROM GRID SWEEP
    optimizer:
      name: "adam"
      kwargs:
        lr: ???           # FILL FROM GRID SWEEP
        weight_decay: 0.0
    criterion:
      name: "nll"
      kwargs: {eps: 1e-8}
    metrics: {"genloss": 1, "acc": 1}

hydra:
  sweep:
    dir: outputs/seed_sweep/gen/legendre/d15D40c64/${dataset.name}_${now:%d%m}
    subdir: ${hydra.job.num}
  sweeper:
    params:
      tracking.seed: range(1, 4)
