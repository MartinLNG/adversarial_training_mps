# @package _global_
experiment: seed_sweep

defaults:
  - override /born: fourier/d4D3
  - override /dataset: 2Dtoy/circles_4k
  - override /trainer/ganstyle: null
  - override /trainer/adversarial: null
  - override /tracking: online

tracking:
  seed: 42
dataset:
  split_seed: 11
  gen_dow_kwargs:
    seed: 25

trainer:
  classification:
    max_epoch: 300
    batch_size: 256
    patience: 300
    stop_crit: "clsloss"
    watch_freq: 1000
    save: true
    optimizer:
      name: "adam"
      kwargs:
        lr: 0.021609636715243675
        weight_decay: 3.445426647309657e-07
    criterion:
      name: "negative log-likelihood"
      kwargs:
        eps: 1e-8
    metrics: {"clsloss": 1}

hydra:
  sweeper:
    params:
      tracking.seed: range(1,21)
