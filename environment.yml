name: tn_adv_train
channels:
  - nvidia    # for GPU CUDA packages
  - pytorch
  - defaults
dependencies:
  - python=3.10
  - pytorch=2.0.*        # stable with TensorKrowch
  - torchvision
  - torchaudio
  - matplotlib
  - jupyter
  - ipykernel
  - numpy=1.25.*          # compatible with PyTorch 2.0
  - scipy
  - scikit-learn
  - tqdm
  - mlflow
  - pip
  - pip:
      - tensorkrowch
      - opt_einsum==3.2
      - hydra-core

